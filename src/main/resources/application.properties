spring.datasource.url=${APP_DB_URL:jdbc:h2:file:./data/assignment08-exercise05;MODE=MySQL;DB_CLOSE_ON_EXIT=FALSE}
spring.datasource.username=${APP_DB_USERNAME:sa}
spring.datasource.password=${APP_DB_PASSWORD:}
spring.datasource.driverClassName=${APP_DB_DRIVER:org.h2.Driver}

spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=${APP_JPA_DDL_AUTO:update}
spring.jpa.properties.hibernate.dialect=${APP_JPA_DIALECT:org.hibernate.dialect.H2Dialect}

spring.thymeleaf.cache=false
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=6MB

app.upload.dir=/home/thien/uploads/assignment08-exercise05-graphql
app.demo.seed=${APP_DEMO_SEED:false}
app.demo.password=${APP_DEMO_PASSWORD:}
app.product.page-size=6
app.mail.mock=true
app.mail.host=
app.mail.port=587
app.mail.username=
app.mail.password=
app.mail.from=no-reply@example.com
app.mail.auth=true
app.mail.starttls=true
app.mail.ssl=false
